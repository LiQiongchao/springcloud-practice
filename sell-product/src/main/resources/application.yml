

spring:
  jpa:
    show-sql: true
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sell?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=UTC
    username: root
    password: root
  application:
    name: sell-product
eureka:
  client:
    service-url:
      defaultZool: http://127.0.0.1:8761/euraka/
server:
  port: 8080
